import styled from 'styled-components';

const VideoBackground = {};

VideoBackground.Video = styled.video`
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;
  min-width: 100%; 
  min-height: 100%;
`;

export default VideoBackground;
